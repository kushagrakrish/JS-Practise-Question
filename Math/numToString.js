// JavaScript Number toFixed( ) method in JavaScript is used to format a number using fixed-point notation. It can be used to format a number with a specific number of digits to the right of the decimal.

const number = 3.114566;

const numStr = number.toFixed(4);

console.log(numStr);

let number2 = 32;
const newNumStr = number2.toString();
console.log(newNumStr);
